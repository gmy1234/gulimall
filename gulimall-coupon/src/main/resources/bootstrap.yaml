spring:
  application:
    name: gulimall-coupon
  cloud:
    nacos:
      config:
        server-addr: 192.168.154.128:8848
        file-extension: yaml
        # coupon  模块下的命名空间，必须是 ID
        namespace: 6dda8750-2888-4965-b1cb-97c3e12b200b
        group: DEFAULT_GROUP
        extension-configs[0]:
          data-id: datasource.yaml
          group: dev
          refresh: true   # 是否动态刷新，默认为false
        extension-configs[1]:
          data-id: MyBatis.yaml
          group: dev
          refresh: true
        extension-configs[2]:
            data-id: other.yaml
            group: dev
            refresh: true


